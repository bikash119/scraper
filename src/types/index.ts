/**
 * Type definitions for the land records data structure
 * @module types
 */

// Re-export all types from the state module
export * from './state.js';

// This barrel file provides a single import point for all type definitions
// Example usage: import { State, District, Plot } from '@/types';

// Add any additional type exports here 